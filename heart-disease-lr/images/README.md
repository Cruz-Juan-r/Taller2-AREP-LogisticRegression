# SageMaker screenshots

Replace this file with the three screenshots captured while running
`sagemaker/sagemaker_heart_lr.ipynb` inside the AWS Academy SageMaker notebook instance.
The filenames below are the ones embedded in the root `README.md`:

| File | What to capture |
|---|---|
| `sagemaker_notebook.png` | The notebook open in the SageMaker notebook instance, with the `estimator.fit()` cell running (log streaming). |
| `sagemaker_training_job.png` | Evidence that training finished: either the log tail showing `Training job completed`, or the *SageMaker → Training → Training jobs* console page with status **Completed**. |
| `sagemaker_test_metrics.png` | The output of section 5 of the notebook: the test-set accuracy / precision / recall / F1 table. |

Do **not** include endpoint or inference-endpoint evidence — endpoint deployment is outside the
scope of the AWS Academy account used in this course.
